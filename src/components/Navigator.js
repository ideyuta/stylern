/* @flow */
import common from '../common';

const Navigator = {
  'Bar': {
    backgroundColor: '#fff',
    borderBottomWidth: 0,
    shadowColor: common.color.gray900,
    shadowOffset: {
      height: 0,
      width: 0
    },
    shadowOpacity: 0.2
  },
  'Scene': {paddingTop: 64},
  'Scene-gray': {backgroundColor: common.color.gray20}
};

export default Navigator;
